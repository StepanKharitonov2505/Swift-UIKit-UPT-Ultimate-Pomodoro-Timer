import UIKit

enum ColorSet {
    
    enum TabBarColors {
        static let tabBarBackgroundColor: UIColor = UIColor.rgb(249, 148, 22)
        static let tabBarSelectItemTintColor: UIColor = UIColor.rgb(77, 76, 125)
        static let tabBarUnselectItemTintColor: UIColor = UIColor.rgb(7, 118, 166)
        
        enum ControlPanelColors {
            static let controlPanelBackgroundColor = UIColor.rgb(255, 255, 255)
            static let controlPanelSeparatorColor = UIColor.rgb(235, 235, 235)
        }
    }
    
    enum FoundationColors {
        static let mainBackgroundColor: UIColor = UIColor.rgb(33, 29, 63)
        static let titleColor: UIColor = UIColor.rgb(255, 255, 255)
        static let actionButtonsColor: UIColor = UIColor.rgb(246, 249, 228)
    }
    
    enum TimerScreenElementsColors {
        
    }
}
